# BACKLOG - matchkit
src/match_type.rs:127 | bug | low | Match::is_empty returned false for inverted matches (start>end) though len()==0. | status=done | FIXED: is_empty now uses self.start >= self.end, consistent with len()'s saturating_sub. Proving test inverted_match_is_empty_and_zero_length; existing start==end tests still green.
src/match_type.rs:226 | bug | medium | MatchBatch::into_vec indexed starts[i]/ends[i] by pattern_ids.len(); mismatched pub SoA vecs panic. | status=done | FIXED: rewrote into_vec to zip pattern_ids x starts x ends (panic-free, stops at shortest) + debug_assert_eq on the three lengths to surface the broken invariant in test/debug builds. Release is safe; debug is loud. Proving test match_batch_into_vec_round_trips.
src/match_type.rs:180 | bug | low | MatchBatch::with_capacity does not clamp or validate cap before allocation, risking OOM panics on large inputs | FIXED: with_capacity now clamps cap to Self::max_capacity() (isize::MAX/size_of::<u32>()), mirroring MatchSet, which prevents the capacity-OVERFLOW panic. KEY INSIGHT: an INFALLIBLE with_capacity can never be fully OOM-safe (a huge-but-sub-overflow cap still aborts in Vec::with_capacity on alloc failure), so the real fix is the OOM-safe surface: added MatchBatch::try_with_capacity + try_push (return Error::OutOfMemory, never abort). Proving test try_with_capacity_is_reasonable_and_fails_loud_on_hostile_size asserts try_with_capacity(usize::MAX) => Err(OutOfMemory) for BOTH types; with_capacity_sane_hint_starts_empty_with_capacity covers the trusted path. | status=done
src/match_set.rs:78 | bug | medium | try_extend reserved only size_hint.lower then push()ed the rest, so elements beyond the hint could abort on OOM. | status=done | FIXED: added try_reserve(1) before each push in the loop (no-op when spare capacity exists), so every element is OOM-safe. Lib green.
src/match_set.rs:47 | bug | low | MatchSet::with_capacity clamps cap to isize::MAX / 12 which is far too large and still panics on OOM | status=done | RESOLVED by design: the isize::MAX/size_of clamp (now via the shared Self::max_capacity() helper, ONE-PLACE with MatchBatch) is the CORRECT guard for the infallible constructor — it prevents the capacity-OVERFLOW panic. A large-but-valid cap that exhausts RAM is inherent to any infallible allocator; callers with untrusted sizes must use the existing OOM-safe MatchSet::try_with_capacity (returns Error::OutOfMemory). Documented both constructors accordingly. Proving test with_capacity_clamps_hostile_size_without_panic (with_capacity(usize::MAX) -> empty, no panic).
src/match_type.rs:115 | bug | low | Match::overlaps returned true for inverted ranges (10..5). | status=done | FIXED: overlaps now returns false if either range is inverted (start>end) before the interval test; valid ranges (incl empty start==end) are unchanged. Proving tests inverted_match_does_not_overlap + valid_overlapping_matches_still_overlap (regression). Note: contains was NOT flagged and is less prone (contains checks self.start<=other.start first); left as-is to avoid scope creep.
Cargo.toml:42 | perf | medium | Redundant integration test targets declared in Cargo.toml are also path-referenced in tests/unit/all.rs causing duplicate compilations | FIXED: removed the 5 standalone [[test]] targets (unit, unit_depth, adversarial, integration, property) that tests/unit/all.rs (the unit_all aggregate runner) already #[path]-includes, so each file compiles ONCE. Kept the targets NOT in all.rs (unit_test_matchkit, adversarial_break_it, campaign_*). Verified coverage preserved: unit_all now runs 151 aggregated tests, full suite green. | status=done
tests/regression/mod.rs:1 | stub | low | tests/regression/mod.rs contains only doc comments and no test cases | FIXED (Law 2, stub removed): populated with public-API regression tests: inverted_match_is_empty_zero_length_and_non_overlapping (:127/:115), match_batch_into_vec_round_trips_aos (:226), with_capacity_sane_hint_starts_empty_with_capacity, try_with_capacity_is_reasonable_and_fails_loud_on_hostile_size, try_push_keeps_soa_columns_consistent. All assert concrete values. | status=done
src/match_type.rs:162 | capability | medium | MatchBatch lacks try_with_capacity and try_push methods for OOM-safe operations | FIXED: added MatchBatch::try_with_capacity (per-column try_reserve -> Error::OutOfMemory, never aborts) + try_push (reserves all 3 SoA columns before pushing any, so a failed reservation leaves columns equal-length). Consistent with the existing MatchSet::try_* surface. Proving tests try_with_capacity_is_reasonable_and_fails_loud_on_hostile_size + try_push_keeps_soa_columns_consistent. | status=done
tests/adversarial/mod.rs:521 + tests/unit/depth.rs:373 | test-truth | medium | DISCOVERED while wiring the regression suite + de-duping test targets: two STALE tests asserted `!m.is_empty()` for an inverted range Match(_,100,50)/(_,20,10), contradicting the match_type.rs:127 fix that made is_empty use `start >= end` (inverted => empty). They only surfaced once unit_all actually ran them (the standalone targets masked / the suite was not re-run after the is_empty fix). | FIXED: both now assert m.is_empty() with a comment tying to the fix; matches the new regression test. Law 6/9: corrected tests asserting the buggy pre-fix shape, not weakened. | status=done
DONE 2026-07-14 | dedup | low | ONE-PLACE: `Match` exposes two identical 3-arg constructors — `new` (canonical) and `from_parts` (delegates to `new`), and from_parts' doc comment is self-referential ("Legacy constructor (compatibility with from_parts)"). Published 0.1.1 shipped from_parts only; `new` was added after. Both are now on crates.io as of 0.1.1+, so keep both for compat but fix the doc. | Correct from_parts doc to "Legacy alias for `new`, retained for API compatibility"; do NOT remove either (published surface). Optionally standardize call sites on `new`. | status=open (metadata-only; defer to next matchkit patch to avoid an isolated irreversible republish) | FIXED: corrected `from_parts` doc comment to "Legacy alias for `Match::new`, retained for API compatibility" in `src/match_type.rs`. `cargo test -p matchkit` green.